const n="# assets — 图片集中目录（Obsidian vault 附件默认位置）\n\n## 规则\n\n- **所有文章配图统一放这里**，Obsidian 设置：文件与链接 → 附件默认存放位置 → 指定文件夹 → `assets`。\n- 写作时引用用 vault 根相对路径：`![示意图](assets/fig-1.png)`（Obsidian「最短路径」格式，站点已支持解析）。\n- 命名：**英文小写连字符**，如 `fig-1.png`、`pipeline.png`、`structure-overview.svg`。避免中文文件名（URL 编码兼容问题）。\n\n## 发布前：打包文章\n\n写作期图片集中在 `assets/`；发布前用打包命令把引用到的图片**复制到文章 md 同目录**并改写引用，使文章自包含：\n\n```bash\nnpm run data:pack -- notes/Programming/bash/bash-scripting   # 打包单篇\nnpm run data:pack                                            # 无参数：递归打包全部文章\n```\n\n- 图片复制到 `notes/Programming/bash/` 下；与已有文件同名时自动加 slug 前缀（如 `bash-scripting-fig-1.png`）。\n- 同时改写 `cache/en` 英文镜像的引用（不复制文件，渲染时复用同一张图）。\n- 幂等：已打包的文章再次运行无副作用。\n";export{n as default};
